# Public — safe to expose to the browser, protected by RLS not secrecy (doc 05)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — NEVER used in a user-facing request path (doc 05).
# Reserved exclusively for migrations/admin scripts run by Dan directly.
# SUPABASE_SERVICE_ROLE_KEY=
